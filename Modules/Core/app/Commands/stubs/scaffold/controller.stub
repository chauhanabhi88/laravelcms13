<?php

namespace Modules\$STUDLY_NAME$\Http\Controllers\Backend;

use Illuminate\Http\Request;
use Illuminate\Http\Response;
use Modules\$STUDLY_NAME$\Models\$STUDLY_NAME$;
use Modules\$STUDLY_NAME$\Repositories\$STUDLY_NAME$Repository;
use Modules\$STUDLY_NAME$\Http\Requests\CreateRequest;
use Modules\$STUDLY_NAME$\Http\Requests\UpdateRequest;
use Modules\Core\Http\Controllers\BackendController;
use Modules\Menu\Models\Menu;

class IndexController extends BackendController
{
    /**
     * @var $STUDLY_NAME$Repository
     */
    private $$LOWER_NAME$;

    /**
      * @var $$LOWER_NAME$Entity
     */
    private $$LOWER_NAME$Entity;

    public function __construct($STUDLY_NAME$Repository $$LOWER_NAME$Repo, $STUDLY_NAME$ $$LOWER_NAME$)
    {
        parent::__construct();

        $this->$LOWER_NAME$ = $$LOWER_NAME$Repo;
        $this->$LOWER_NAME$Entity = $$LOWER_NAME$;
    }
    
    /**
     * Display a listing of the resource.
      * @return Response
     */
    public function index(Request $request)
    {
        try
        {
            if (function_exists('getPerPageForModule')) {
                $perPage = getPerPageForModule(config("$LOWER_NAME$.cache.name"), $request->get("per_page"));
                $request->merge(['per_page' => $perPage]);
            }
            $routeName = $request->route()?->getName();
            $menuId = Menu::where('link', $routeName)->first()->id;
            $columns = getColumnObject()->getColumns($menuId);
            $collection = $this->$LOWER_NAME$->pagination($request);
            $filters = $this->$LOWER_NAME$->getFilters($request);
            $statusOptions = $this->$LOWER_NAME$->getStatusOptions(true);
            $CONTROLLER_INDEX$
            return view('$LOWER_NAME$::backend.index', compact('request', 'collection', 'columns', 'filters','statusOptions'));
        }
        catch (\Throwable $e) {
            return redirect()->route('admin.dashboard.index', updateUrlParams())->with("error", $e->getMessage());
        }
    }

    /**
     * Display a listing of the resource.
     *
     * @return Response
     */
    public function filters(Request $request)
    {
        try {
            if (function_exists('getPerPageForModule')) {
                $perPage = getPerPageForModule(config("$LOWER_NAME$.cache.name"), $request->get("per_page"));
                $request->merge(['per_page' => $perPage]);
            }
            setFilterSession(config("$LOWER_NAME$.cache.name"), $request);
            $activeMenuId = $request->get('active_menu_id');
            $columns = getColumnObject()->getColumns($activeMenuId);
            $filters = $this->$LOWER_NAME$->getFilters($request);
            $collection = $this->$LOWER_NAME$->pagination($request);
            $statusOptions = $this->$LOWER_NAME$->getStatusOptions(true);
            $CONTROLLER_INDEX$
            $content = view('$LOWER_NAME$::backend.partials.grid', compact('request', 'collection', 'columns', 'filters','statusOptions'));
            return response()->json([
                'type' => 'success',
                'content' => [
                    'element' => 'collection',
                    'html' => $content->__toString()
                ],
                'message' => $request->get('message')
            ]);
        } catch (\Exception $e) {
            return response()->json([
                'type' => 'error',
                'message' => $e->getMessage()
            ]);
        }
    }

    /**
     * Show the form for creating a new resource.
     * @return Response
     */
    public function create()
    {
        try
        {
$CONTROLLER_DATA$
            return view('$LOWER_NAME$::backend.create' $CONTROLLER_VARIABLE$);
        }
        catch (\Throwable $e)
        {
            return redirect()->route('admin.$LOWER_NAME$.index', updateUrlParams())->with("error", $e->getMessage());
        }
    }

    /**
     * Store a newly created resource in storage.
     * @param Request $request
     * @return Response
     */
    public function store(CreateRequest $request)
    {
        try
        {
            $params = $request->all();
$CONTROLLER_STORE$
            $$LOWER_NAME$ = $this->$LOWER_NAME$->create($params['$LOWER_NAME$']);
            if(isset($params['snc']) && $params['snc']) {
                return redirect()->route('admin.$LOWER_NAME$.edit', updateUrlParams([$$LOWER_NAME$->id]))->with("success", trans("$LOWER_NAME$::$LOWER_NAME$.messages.created_success"));
            }
            return redirect()->route('admin.$LOWER_NAME$.index', updateUrlParams())->with("success", trans("$LOWER_NAME$::$LOWER_NAME$.messages.created_success"));
        } catch (\Throwable $e) {
            return redirect()->route('admin.$LOWER_NAME$.create', updateUrlParams())->with("error", $e->getMessage());
        }
    }

    /**
     * Show the form for editing the specified resource.
     * @param int $id
     * @return Response
     */
    public function edit(Request $request)
    {
        try {
            $id = $request->id;
            if (!$id) {
                throw new \Exception(trans("$LOWER_NAME$::$LOWER_NAME$.messages.data_invalid"));
            }
            $$LOWER_NAME$ = $this->$LOWER_NAME$->find($id);
            if(!$$LOWER_NAME$) {
                throw new \Exception(trans("$LOWER_NAME$::$LOWER_NAME$.messages.data_invalid"));
            }
$CONTROLLER_DATA$
            return view('$LOWER_NAME$::backend.edit', compact('$LOWER_NAME$' $CONTROLLER_EDIT_VARIABLE$));
        } catch (\Throwable $e) {
            return redirect()->route('admin.$LOWER_NAME$.index', updateUrlParams())->with("error", $e->getMessage());
        }
    }

    /**
     * Update the specified resource in storage.
     * @param Request $request
     * @param int $id
     * @return Response
     */
    public function update(UpdateRequest $request)
    {
        try
        {
            $id = $request->id;
            if (!$id) {
                throw new \Exception(trans("$LOWER_NAME$::$LOWER_NAME$.messages.data_invalid"));
            }
            $params = $request->all();
            $$LOWER_NAME$ = $this->$LOWER_NAME$->find($id);
            if(!$$LOWER_NAME$) {
                throw new \Exception(trans("$LOWER_NAME$::$LOWER_NAME$.messages.data_invalid"));
            }
$CONTROLLER_UPDATE$
            $this->$LOWER_NAME$->update($$LOWER_NAME$, $params['$LOWER_NAME$']);
            if(isset($params['snc']) && $params['snc']) {
                return redirect()->route('admin.$LOWER_NAME$.edit', updateUrlParams([$id]))->with("success", trans("$LOWER_NAME$::$LOWER_NAME$.messages.updated_success"));
            }
            return redirect()->route('admin.$LOWER_NAME$.index', updateUrlParams())->with("success", trans("$LOWER_NAME$::$LOWER_NAME$.messages.updated_success"));
        } catch (\Throwable $e) {
            return redirect()->route('admin.$LOWER_NAME$.edit', updateUrlParams([$id]))->with("error", $e->getMessage());
        }
    }

    /**
     * Remove the specified resource from storage.
     * @param int $id
     * @return Response
     */
    public function delete(Request $request)
    {
        try {
            $id = $request->id;
            if (!$id) {
                throw new \Exception(trans("$LOWER_NAME$::$LOWER_NAME$.messages.data_invalid"));
            }
            $$LOWER_NAME$ = $this->$LOWER_NAME$->find($id);
            if(!$$LOWER_NAME$) {
                throw new \Exception(trans("$LOWER_NAME$::$LOWER_NAME$.messages.data_invalid"));
            }

            $this->$LOWER_NAME$->destroy($$LOWER_NAME$);
            return redirect()->route('admin.$LOWER_NAME$.index', updateUrlParams())->with("success", trans("$LOWER_NAME$::$LOWER_NAME$.messages.deleted_success"));
        } catch (\Throwable $e) {
            return redirect()->route('admin.$LOWER_NAME$.index', updateUrlParams())->with("error", $e->getMessage());
        }
    }

    /**
     * Remove Selected / All resource from storage
     */
    public function massDelete(Request $request){
        try {
            $this->$LOWER_NAME$->destroyMultiple($request);
            return redirect()->route('admin.$LOWER_NAME$.index', updateUrlParams())->with("success", trans("$LOWER_NAME$::$LOWER_NAME$.messages.deleted_success"));
        } catch (\Throwable $e) {
            return redirect()->route('admin.$LOWER_NAME$.index', updateUrlParams())->with("error", $e->getMessage());
        }
    }

     public function updateStatus(Request $request){
        if($request->get('id')){
            $id = $request->get('id');
            $status = $request->get('status');
            $$LOWER_NAME$Row = $this->$LOWER_NAME$->find($id);
            $status = ($status == 1)? config('core.enabled') : config('core.disabled');
            $params = array('status'=>$status);
            $this->$LOWER_NAME$->update($$LOWER_NAME$Row, $params);
        }
        $request = new Request();
        $request->merge(['message' => trans("core::core.messages.status_change_success")]);
        return $this->filters($request);
    }
}
